#!/usr/bin/env bash
# multiAgentChat CLI shim
# Always resolves to the package root so `agent` works from any cwd.
set -e
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT="$(cd "$HERE/.." && pwd)"
exec npx --prefix "$ROOT" tsx "$ROOT/src/control/cli.ts" "$@"
