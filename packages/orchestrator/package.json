{
  "name": "multiagent-orchestrator",
  "version": "0.0.1",
  "private": true,
  "description": "Pure task-state / stage / gate / memory / approval logic. IM-agnostic, host-agnostic.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tasks": "./src/tasks/index.ts",
    "./presets": "./src/presets/index.ts",
    "./memory": "./src/memory/index.ts",
    "./approval": "./src/approval/index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
